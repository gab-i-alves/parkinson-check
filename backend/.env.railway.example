# ============================================
# RAILWAY ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Este arquivo mostra TODAS as variáveis que você precisa configurar no Railway
# Copie cada linha e adicione no painel de Environment Variables do serviço backend

# ============================================
# 1. DATABASE (do serviço PostgreSQL Railway)
# ============================================
DATABASE_URL=postgresql+psycopg://postgres:PASSWORD@HOST:PORT/railway
# Exemplo: postgresql+psycopg://postgres:senha123@containers-us-west-xxx.railway.app:7432/railway
# IMPORTANTE: Substitua pelo DATABASE_URL fornecido pelo Railway (aba "Variables")

# ============================================
# 2. JWT & SECURITY
# ============================================
SECRET_KEY=seu-secret-key-super-seguro-aqui-min-32-chars
# GERE UMA CHAVE SEGURA: python -c "import secrets; print(secrets.token_urlsafe(32))"
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REMEMBER_ME_EXPIRE_DAYS=30

# ============================================
# 3. ENVIRONMENT
# ============================================
ENVIRONMENT=production

# ============================================
# 4. SMTP (Email)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app-do-gmail
# Para Gmail: https://support.google.com/accounts/answer/185833
# Use "App Password" (senha de app), NÃO a senha normal

# ============================================
# 5. ML SERVICES (URLs internas do Railway)
# ============================================
SPIRAL_CLASSIFIER_URL=http://spiral-classifier.railway.internal:8001
# Após criar o serviço spiral-classifier, use o formato:
# http://NOME-DO-SERVICO.railway.internal:PORTA

VOICE_CLASSIFIER_URL=http://voice-classifier.railway.internal:8002
# Após criar o serviço voice-classifier, use o formato:
# http://NOME-DO-SERVICO.railway.internal:PORTA

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. NÃO commit este arquivo com valores reais no Git
# 2. No Railway, adicione cada variável individualmente no painel
# 3. As URLs dos ML services devem usar o domínio .railway.internal
# 4. O DATABASE_URL é gerado automaticamente pelo Railway quando você conecta o Postgres
# 5. Para verificar as variáveis: railway variables list (via CLI)
